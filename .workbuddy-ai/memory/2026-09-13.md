# 2026-09-13

## Điều tra giật lag Reader sau khi bấm "Cập nhật" ở màn Dịch

**Yêu cầu:** vì sao sau khi nhấn Cập nhật trong màn Dịch vài lần thì vuốt Reader giật lag; mở màn Dịch giật; nới/thu token giật. Cần bằng chứng điều tra + phương pháp giải quyết.

**Kết quả:** viết `Docs/Reports/2026-09-13-reader-translation-panel-jank.md`. **Không sửa code.** `validate_links.py` PASS (16 docs, 526 file Swift).

### Chuỗi nhân quả đã xác minh

- Nút "Cập nhật" = `saveDefinition` (`ReaderView+DefinitionLoading.swift:26-49`) → `TranslationDictionaryWriter.mutate` (parse + ghi lại **cả file** TXT custom, dựng lại frozen trie từ **tất cả** record) → `notifyDictionariesDidUpdate` → `TranslateUtils.invalidateCache` xoá **cả 4** bộ cache → Reader dịch lại **cả chương** → mọi `ReaderTextView` đang hiện dựng lại attributed string + `invalidateIntrinsicContentSize`.
- **Phạm vi mặc định là "R" (Riêng)**, không phải toàn cục: `ReaderView.swift:123` `@AppStorage("pinnedSaveToBookSpecific") = true`, `:125` `@State saveToBookSpecific = true`. Nên đường mặc định chỉ bump `bookGenerations[bookId]`.
- `updateCachedTranslatedContent` (`ReaderViewModel+Translation.swift:203-214`) đặt `translationToken = 0` cho **mọi** chương đã cache khác ⇒ `requestChapter` (`ReaderViewModel.swift:454-480`) rơi khỏi đường nhanh trong RAM ⇒ N lần lật trang sau đều **tải lại nội dung từ extension/DB**.
- `ChapterCache.queueReleaseAllNonVisible` (`ChapterCache.swift:153`) **không có call site** ⇒ cache chương chỉ dọn khi Memory Warning (`ReaderViewModel.swift:678-681`).
- Panel Dịch: `ReaderDefinitionOverlayView.swift:178-217` dựng **một `Text` cho mỗi đơn vị UTF-16 của cả đoạn** trong `HStack` **không lazy** ⇒ nguồn chính của lag khi mở panel và mỗi lần nới/thu.
- **Phát hiện mới:** `TranslationWordToken.id = UUID()` (`TranslationWordToken.swift:4`) ⇒ mỗi lượt nạp token là toàn bộ danh tính đổi ⇒ `ForEach` phá hủy/dựng lại cả hàng, và `.onChange(of: translationTokens.map(\.id))` (`ReaderDefinitionOverlayView.swift:277`) **luôn** nổ (đây là bản vá 1.3.373, nay thành nguồn animation thừa).

### Đính chính báo cáo cũ `Docs/Reports/2026-09-10-reader-tts-perf-review.md`

- **RC-5 (rò rỉ NSCache không giới hạn) — ĐÃ SỬA, không còn đúng.** `TranslateUtils.swift:5` giờ là `TranslationMemo<String>(maxEntries:1024, maxCost:8MB)`; `:897` có `translationCache.invalidate(bookId:)`.
- **RC-9 (`applyTranslation` kích objectWillChange) — SAI.** `ReaderViewModel.swift:143` là `var` thường, không `@Published`.
- **P-2 (mở panel tokenize nguội trên MainActor) — SAI.** `ReaderDefinitionWorker` là `actor` (`:4`), `getTranslationTokens` không `@MainActor` ⇒ chạy off-main.
- **Còn đúng:** RC-3 (dịch lại cả chương, `scope.term` mang `word` nhưng không ai đọc), RC-6, `ChapterCache` không trần, cache rule engine chỉ 64 entry (`QuickTranslationRuleEngine.swift:55`), P-1, P-3.

### Kế hoạch sửa đề xuất (theo thứ tự)

P0-1 bỏ N view ký tự ở panel (dùng `UIViewRepresentable` + `layoutManager.characterIndex(for:)` như `ReaderTextView.swift:214-221`) → P0-3 lật trang dịch lại từ `cached.originalContent` thay vì tải lại → P0-4 gọi `queueReleaseAllNonVisible` sau `commitNavigation` → P0-2 chỉ dịch lại đoạn có `item.original.contains(word)` (bộ lọc bảo thủ) → P1-1 id token ổn định → P1-2 debounce `loadDefinitionData` theo vùng chọn → P1-3/P1-4/P2.

### Bài học dùng lại

- Trong repo này, **đọc lại báo cáo cũ trước khi điều tra lại** — một phần đã được sửa và một phần đã sai; nếu không đối chiếu sẽ đi tìm nguyên nhân ở chỗ không còn.
- `[ReaderEnergy] Summary` và `[ReaderPerf] TranslationRefresh` là log đã có sẵn, nhưng `ReaderEnergyDiagnostics.isEnabled` **chốt một lần** ở `beginReaderSession()` ⇒ phải bật log **trước khi mở Reader**.

## Bổ sung 19:10 — kiểm chứng `implementation_plan.md` của agent khác

Người dùng đưa `c:\Users\hoang\.gemini\antigravity\brain\8a2a8ec6-...\implementation_plan.md` (plan của Gemini/Antigravity cho cùng vấn đề) và hỏi đánh giá + xác nhận giả thuyết **máy nóng**.

### Phát hiện MỚI, đã kiểm chứng toàn bộ — `FrozenTrieDictionary` nhánh `dat == nil` cấp phát `String` mỗi độ dài, mỗi vị trí ký tự

- `FrozenTrieDictionary.swift:33-54`: `if let dat { trieMatches(...) }` — nhanh, dừng sớm. Ngược lại duyệt `lengths` và `String(decoding:)` **mỗi vòng**. Ngoài ra `trieMatches` **cũng** cấp phát `Array(text.utf16)` mỗi lần gọi (`:59`).
- **Từ điển nào `dat == nil`** (mấu chốt, đã truy tận nơi):
  - `namesDict` / `vietPhraseDict` / `pronounsDict` / `luatNhanDict` → `DoubleArrayTrie.frozen()` (`DoubleArrayTrie.swift:44-47`) → **nhanh**.
  - `customNamesDict` / `customVietPhraseDict` → `TextDictionary.frozen()` (`TextDictionary.swift:24-26`) → **chậm**.
  - **`bookVP` / `bookNames` (từ điển riêng truyện)** → `TextDictionary.frozen()` qua `TranslationDictionaryState.book(...)` (`:83-89`) → **chậm**.
- **Vì sao "sau khi Cập nhật" mới giật:** trước lần lưu đầu tiên file chưa tồn tại ⇒ dict `nil` ⇒ nhánh `if let customVP` / `if let bookVP` **bị bỏ qua** (`VietPhraseTokenizer.swift:81, 166, 172`). Sau lần lưu đầu tiên ⇒ khác `nil` ⇒ **bật vĩnh viễn** (`TranslationManager+Publication.swift:9-11`: `entries.isEmpty ? nil : FrozenTrieDictionary(...)`).
- **Với phạm vi mặc định "R"**, từ điển được bật là **`bookVP`/`bookNames`** (file `books/<id>/VietPhrase.txt`), không phải `custom*` như plan nói — cùng cơ chế, khác từ điển.
- **Phép thử phân biệt** (rẻ, sức nặng nhất): chưa bấm Cập nhật → ghi `[ReaderPerf] TranslationRefresh totalMs`; bấm Cập nhật 1 từ → lật vài chương, ghi lại (**phải tăng vọt và không quay về**); xoá từ điển riêng truyện → lật lại (**phải quay về mốc nền**). Nếu quay về ⇒ §2.4 đúng, không phải "cache nguội".

### Đánh giá plan của agent kia (đã kiểm chứng từng bằng chứng)

- **Đúng và giá trị nhất:** Bằng chứng 1 (allocation storm) — báo cáo bản đầu của tôi bỏ sót.
- **Đúng về cơ chế nhưng phóng đại tần suất:** Bằng chứng 2 — `ReaderViewModelInvalidationRelay` **có thật** (`:26-43`) và forward `objectWillChange` (`:40-42`), nhưng `updateScrollReadingProgress` debounce 200 ms (`ReaderView.swift:1802-1816`) **và** `updateProgress` early-return khi vị trí không đổi (`ReaderViewModel.swift:324`).
- **SAI về bậc độ lớn:** Bằng chứng 4 — `Global` toàn `Dictionary`/`Set`/`Array` ⇒ copy-on-write, gán struct là **retain O(1)**, **không** deep-copy 100.000 mục. Chi phí thật chỉ là 8 lần `NSLock` mỗi `tokenizeUncached` (`VietPhraseTokenizer.swift:49-56`) ⇒ P2, không phải P0.
- **Đúng triệu chứng, sai gốc:** Bằng chứng 5 — gốc là **`TranslationWordToken.id = UUID()`** (`TranslationWordToken.swift:4`), plan không thấy dòng này; chỉ bỏ `.onChange(of: map(\.id))` là workaround và **rủi ro regression** (chính nó là bản vá 1.3.373 cho lỗi "lúc cuộn được lúc không").
- **Thiếu 4 thứ:** (1) hàng câu gốc N view ký tự `ReaderDefinitionOverlayView.swift:182-202` — **nguyên nhân số 1 của "mở panel giật"**, plan bỏ qua hoàn toàn; (2) `updateCachedTranslatedContent` đánh stale mọi chương đã cache; (3) `ChapterCache` không trần; (4) `GeometryReader` mỗi đoạn với `.onChange(of: geo.frame(in: .global))` (`ReaderView.swift:1426-1435`).
- **BẪY KỸ THUẬT trong "Giải pháp 2" của plan:** nó đề nghị truyền chuỗi gốc + `startIndex: i` thay vì cắt `checkText`. **Làm vậy sẽ sai kết quả** nếu không thêm `maxLength`: biên `limit`/`maxLimit` là **ngữ nghĩa** (chặn VP khớp xuyên qua mốc bắt đầu của tên riêng đã chọn — `VietPhraseTokenizer.swift:70, 159-160`), còn `trieMatches` quét tới hết chuỗi (`FrozenTrieDictionary.swift:63`). Chữ ký mới **bắt buộc** phải có `maxLength`.

### Cập nhật báo cáo

`Docs/Reports/2026-09-13-reader-translation-panel-jank.md` (377 dòng) đã thêm: §2.4 (allocation storm), §5.5 (đánh giá plan kia + bảng phán quyết), **P0-0** (viết lại tra cứu trie zero-allocation), P2-4/P2-5, và thí nghiệm phân biệt ở §7. `validate_links.py` PASS 100%.

## Bổ sung 19:15 — plan được cập nhật lúc 19:11, đã đối chiếu chương mẫu thật

Người dùng gửi lại cùng đường dẫn. Kiểm `md5sum`/mtime thấy file **đã sửa lúc 19:11** (sau khi tôi đọc 19:07) ⇒ đọc lại: plan thêm **§I** (đo trên chương thật do người dùng cung cấp) và thêm **PrefetchManager** vào Bằng chứng 2.

**Đã đếm lại chính file chương mẫu** `E:\1.DOWNLOADS\Đấu La Võ Hồn bốn tuyển hai, ta nghịch thiên mà đi! - 第1章 ..._20260913190953.txt` (6.061 byte):
- 85 dòng / **83 đoạn không rỗng** (plan nói 84 dòng, 83 đoạn ⇒ đoạn đúng, dòng lệch 1).
- **1.731** chữ Hán thật; **2.011** ký tự không phải khoảng trắng. ⇒ Con số "2.012 ký tự Hán" của plan thực ra là **"ký tự không phải khoảng trắng"** — dán nhãn sai.
- **2.341** = tổng `len(line)` — đây mới là **mẫu số đúng** vì `translateContentWithMapping` chạy theo từng dòng và vòng lặp duyệt **mọi** `Character` kể cả dấu câu/khoảng trắng.
- Trung bình 20,9 CJK/đoạn (plan nói ~24).

**Số lần tra từ điển bị plan đếm THIẾU ~3×:** plan chỉ tính `customNames` + `customVP`, nhưng `VietPhraseTokenizer` gọi `findAllPrefixMatches` cho **mọi** dict khác `nil`: vòng tên = `bookNames`/`customNames`/`names` (+`pronouns`/`luatNhan` nếu bật — **mặc định tắt**, `ReaderView.swift:170-171`); vòng VP = `bookVP`/`customVP`/`vp`. Tối đa **6**/vị trí. Ngược lại plan **thừa 2×** ở khoản `checkText` (tính 8.048, thực tế ~4.682). Và `trieMatches` (đường DAT) **cũng** cấp phát `Array(text.utf16)` (`:59`) — plan không tính.

**Ước lượng lại [bậc độ lớn, không phải đo]:** ~16 cấp phát/vị trí × 2.341 ≈ **37.000/lượt tokenize**; ×2 (nội dung + span, memo đã bị xoá) ≈ **~75.000/lượt dựng chương**. Cùng bậc với ">56.000" của plan ⇒ **kết luận của plan đứng vững**, chỉ chia nhỏ sai.

**Xác minh "prefetch nhân đôi công việc": ĐÚNG.** `commitNavigation` gọi `scheduleSettledPrefetch(after:…, within: [chapterIndex+1])` ở **mỗi** commit (`ReaderViewModel.swift:641`) ⇒ sau mỗi lần Cập nhật, lần **chuyển chương** kế tiếp tốn thêm một lượt dịch lạnh. Plan không phân biệt "chuyển chương" với "vuốt trong chương".

**Kỹ năng dùng lại:** khi người dùng gửi lại **cùng một đường dẫn file** mà không kèm câu hỏi, **luôn kiểm `mtime`/`md5sum` trước** — rất có thể file đã được cập nhật và họ muốn tôi đọc bản mới.

Báo cáo giờ 402 dòng; `validate_links.py` PASS 100%.

## 19:30 — Triển khai 1.3.375 (gói "3 mục rủi ro thấp")

Người dùng chọn gói 3 mục rủi ro thấp. **Đã sửa 4 file nguồn:**

1. **P0-3** — `ReaderViewModel.swift:784-789`: `loadChapterContentFromExtension` thoát sớm trả `.memory` khi `!forceRefresh` + cache có `state == .loaded` + `originalContent` khác rỗng. Lật trang sau khi sửa từ điển không còn tốn vòng `ChapterContentRepository.load`.
2. **P0-4** — `ReaderView.swift:1971-1976`: `applyNavigationCommit` gọi `queueReleaseAllNonVisible(keepIndexes: chapterIndex ± 3)`. **Kèm sửa `ChapterCache.swift:123-141`**: `queueRelease` → `Task { @MainActor }` vì `performRelease` gỡ khoá trong `cache` (`@Observable`). `queueRelease`/`performRelease`/`queueReleaseAllNonVisible` là **code chết** trước lượt này — đánh thức code chết thì phải rà tính an toàn luồng trước.
3. **P1-4 thay cho P0-1** — `QuickTranslationRuleEngine.swift:53-60`: memo rewrite `maxEntries: 64 → 2048` (giữ `maxCost` 2 MiB).

### ĐÍNH CHÍNH quan trọng: P0-1 KHÔNG phải nguyên nhân (đã đo)

`originalSentence` là **MỘT ĐOẠN VĂN**, không phải cả chương (`ReaderView.swift:1494` = `item.original`). Đo trên chính chương mẫu của người dùng: đoạn dài nhất **173** ký tự, trung bình **28**, không có đoạn nào ≥200. ⇒ N view ≈ 28, **không phải** 500–2000 như báo cáo bản đầu viết. Nên **không triển khai P0-1** — sửa chỗ không phải nguyên nhân + đụng cơ chế `scrollTo` mà 1.3.373 vừa ổn định.

**Rủi ro tiềm ẩn vẫn có thật:** `ChapterTextNormalizer.normalizeInternal` (`:24-52`) **chỉ tách theo `\n`** ⇒ nguồn nào trả cả chương thành một dòng thì N lên hàng nghìn. P0-1 hạ xuống P2, chỉ làm khi gặp sách dài dòng thật.

**Bài học:** trước khi sửa một mục "O(N) view", phải xác định N thật bằng cách đọc nguồn dữ liệu (`originalSentence` được gán ở đâu) và đo dữ liệu thật — không suy ra từ tên biến.

### Bẫy của chính `validate_links.py`

`validate_links.py` **short-circuit khi cây làm việc sạch** ⇒ báo PASS dù doc đã stale. Trước lượt này nó PASS, nhưng **10 doc đã stale sẵn từ commit `fbbeef1`** (phiên trước commit mà không `--accept`). Đã kiểm chứng bằng cách `git stash` rồi chạy lại. Cách phân định: `git stash push -- Sources/ Docs/` → chạy validator → `git stash pop`.

⇒ Đừng tin `PASS` của validator khi vừa mới commit. Phải chạy `--explain` **sau khi đã có thay đổi** trong cây làm việc.

### CHANGELOG — 2 vấn đề phát hiện, CHƯA sửa (đã báo người dùng)

1. `CHANGELOG.md` có **43 entry**, vượt mốc ~30 của `AGENTS.md` (đã drift từ trước: 42 trước lượt này).
2. **Thiếu hẳn 1.3.323 → 1.3.328 ở CẢ HAI file** — archive dừng ở `[1.3.322]`, CHANGELOG bắt đầu ở `[1.3.329]`. Sáu version đó vẫn được `08_lifecycle.md` tham chiếu nên đã từng tồn tại ⇒ **một lượt lưu trữ trước đây đã làm mất entry**. Lấy lại được từ `git log`.
   → Không tự đẩy 13 entry sang archive: quy trình lưu trữ đã chứng minh có thể mất dữ liệu.

### Kiểm chứng

- `check_architecture.py`: vẫn **đúng 6 vi phạm nền**, không phát sinh mới. `ReaderViewModel.swift` 918 → 925 (đã vượt baseline 830 sẵn); `ReaderView.swift` 2010 → 2017 (baseline 2053, còn dư); `QuickTranslationRuleEngine.swift` 349/400.
- `validate_links.py`: PASS 100%. `--accept` 7 doc (`04_call_graph`, `05_state_graph`, `08_lifecycle`, `10_risk_report`, `11_subsystems`, `13_resource_lifecycle`, `rules.md`), `--no-change-needed` 3 doc (`03_type_graph`, `07_dataflow`, `12_ownership_graph`).
- **Chưa từng biên dịch** (Windows, không có xcodegen/xcodebuild).
