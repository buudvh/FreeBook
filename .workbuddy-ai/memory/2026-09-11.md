# 2026-09-11

## E-Ink UI plan — re-verified against code, mockups delivered (no code changed)

Plan file: `Docs/Plans/2026-09-10-plan-eink-ui.md` (updated in place with a new §1b "Đối chiếu lại code sau khi bạn sửa").

User said they couldn't picture the result, so I delivered two inline mockup widgets (reader before/after; shelf + settings + full-refresh flash) and then re-verified the plan against the current tree. User explicitly asked to hold off on code changes.

### Re-verification result: plan still valid, 4 deltas

Plan was written at `028e32b`; HEAD is now `44e3a65` plus uncommitted Backup/Telegram work.

Anchors that survived untouched: `ReaderTheme` at `ReaderView.swift:5-49`, `@AppStorage("readerSelectedTheme")` at `ReaderView.swift:173`, chapter-turn gates at `ReaderView.swift:1880`/`:1973`, `SettingsView.swift` still 452 lines (baseline 453 → exactly 1 line of headroom), `ReaderView.swift` still 2002 lines.

Deltas:
1. `animateContent` source moved `ReaderViewModel.swift:654` → **`:639`**.
2. Theme consumers went **12 → 15 files**. Newly surfaced: `Views/Shelf/ShelfMain/ShelfView.swift:95` (also reads `@AppStorage("readerSelectedTheme")`), `Views/Reader/ParagraphCardView.swift:10`, `Views/Reader/ReaderChapterListView.swift:16,36`.
3. `ReaderViewModel.swift` 933 → **918 lines** (better, still over baseline 830 → `check_architecture.py` still red).
4. Swift file count 503 → **523**.

### Two design/process consequences worth remembering
- **`effectiveTheme` cannot live inside `ReaderView`.** `ShelfView.swift:95` reads the same `AppStorage` key, so an e-ink override local to the reader would make the shelf disagree with the reader. It must be a shared helper (`EInkModeSettings.effectiveTheme(for:)` or in `Sources/Common/`).
- **In-flight uncommitted work collides with two plan touch points.** Backup/Telegram is currently editing `Sources/Views/MainTabView.swift` (rename `runAutoDriveBackupIfDue` → `runAutomaticBackupIfDue`) and adds `Sources/Views/Settings/Backup/TelegramBackupSettingsView.swift`. MainTabView is a plan target (e-ink tint); the new Telegram settings screen could consume the single remaining line of `SettingsView.swift` headroom. Re-check `SettingsView.swift` right before wiring the e-ink section.

### Repo facts reconfirmed
- `git status` shows Backup/Telegram as the only in-flight work; no theming work has started.
- Uncommitted CodeGraph docs: `00_index`, `02_file_graph`, `04_call_graph`, `09_dependency_rules`, `10_risk_report`, `11_subsystems`, `14_complexity_report`, `CHANGELOG.md`, `manifest.json` are all modified in the working tree — the CodeGraph bookkeeping for the Backup work is not yet committed.
- `.workbuddy-ai/` and `Scripts/__pycache__/` show as untracked.

## E-Ink plan extended to the remaining screens (§4b added)

User asked what happens to BookDetail, Discovery, Search, Extensions, and Settings subscreens. Full scan of `Sources/Views/**` produced these numbers — useful whenever a themed/display mode is added to this app:

| Area | Files | Colour/accent hits | Low-opacity backgrounds | Hard visual blockers |
|---|---|---|---|---|
| `Reader/` | 46 | 64 | 37 | 3 shadows |
| `Settings/` + subscreens | 51 | 48 | 12 | 1 Material |
| `Shelf/` | 24 | 48 | 4 | 3 Material, 3 shadows |
| `Extensions/` | 11 | 43 | 12 | hardcoded dark editor + 7 syntax colours |
| `BookDetail/` | 12 | 30 | 6 | 6 shadows, 5 coloured pills |
| `TTSWidget/` | 10 | 37 | 4 | 2 Material, 1 LinearGradient, 4 shadows |
| `Dictionary/` | 13 | 14 | 0 | none |
| `Download/` | 2 | 16 | 1 | 2 shadows |
| `Discovery/` | 2 | 11 | 3 | 8 `systemGray` |
| `Search/` | 1 | 9 | 2 | 3 `systemGray` |
| `Common/` (shared) | 29 | 13 | 8 | 1 Material, 1 shadow, dark toast |

**Totals app-wide: 82 `Color.x.opacity(0.05–0.2)` backgrounds, 9 `.ultraThinMaterial`, 1 `LinearGradient`, ~19 `.shadow`.**

### Key conclusions
- **Settings subscreens (51 files) and Dictionary are essentially free** — they use `Form`/`Section`/`Toggle`/`List` + `.accentColor` + semantic colours. A root `.tint(.black)` + `.preferredColorScheme(.light)` + opaque nav/tab appearance clears most of their 48/14 hits without touching a single file.
- **`.ultraThinMaterial` is the worst e-ink offender** (backdrop blur → grey smear). 9 locations: `ShelfView.swift:460,499,522`, `CollectionDetailView.swift:273`, `ShelfSearchView+Actions.swift:62`, `VisibleBrowserReopenView.swift:69`, `TTSFloatingWidgetView.swift:192,244`, `TTSModelManagerView.swift:162`.
- **`Extensions/Editor/*` is the only real outlier**: a hardcoded VS Code-style dark theme (`CodeEditorTextView.swift:9,10,32` bg `#181825`; `HighlightingCodeEditor.swift:53-59` = 7 syntax colours). Three options A/B/C documented; recommended C (light background only, keep syntax colours) and defer B.
- **`RepositoryManagerView.swift` is the worst colour-only-information case** — ~10 badges where type is encoded solely by hue (orange=TTS, purple/green/red=others). Derived a **3-tier badge convention** for monochrome: solid black fill = active/installed, 1px black outline = neutral category, dashed outline + `!` prefix = error. "Convert 4 hues to 4 greys" loses the information entirely.
- **BookDetail's FAB is fine as-is** — `BookDetailView.swift:629,654,675,696,714` has 4 coloured pills, but each already carries a text label + distinct SF Symbol, so monochrome keeps them distinguishable.
- Highest-leverage refactor order: fix shared components first (`BookCoverView`, `ExtensionIconView`, `BookListItemView`, `ToastManager`, `CollectionCoverMosaicView`) — `BookCoverView` alone covers Shelf/BookDetail/Search/Discovery/Dictionary/Download.
- Plan phases renumbered to 0–10, with the global-chrome phase (2) deliberately early because it clears the largest number of hits for the least code.

## Mockups were structurally wrong — corrected by reading the real layouts

User pushed back: "sao mockup chẳng hề giống UI hiện tại tí nào vậy". They were right — the first mockups were drawn from inference, not from the code. Lesson for this repo: **always read the actual view before drawing a mockup.** There are no screenshots anywhere in the repo (only 12 files in `AppIcon.appiconset`), and Windows cannot run the Simulator, so a mockup here can only ever be a **structural wireframe** — correct elements, order, labels, chrome behaviour; it cannot match typography, spacing or SF Symbol detail. Say that out loud instead of implying fidelity.

### Real UI facts worth keeping (correct the earlier assumptions)
- **Navigation is 4 tabs, and there is no Search tab**: `MainTabView.swift:12-37` = Kệ Sách (`books.vertical.fill`) / Khám Phá (`safari.fill`) / Tiện Ích (`puzzlepiece.extension.fill`) / Cài Đặt (`gearshape.fill`). Search lives *inside* Shelf (`ShelfSearchView`, nav title "Tìm trong Kệ sách & Lịch sử") and *inside* the reader (`ReaderSearchView`).
- **`MainTabView.swift:38` already has `.tint(.accentColor)`** — the single app-wide accent lever. So the e-ink `.tint(.black)` is a one-token edit of something that already exists, not a new addition.
- **The reader chrome has no separators at all.** `ReaderHeaderFooterOverlayView.swift:31-33,157,198` uses `selectedTheme.backgroundColor` for both header and footer, so they blend into the page — no rule, no shadow. Header is **two rows**: row 1 = back chevron + `magnifyingglass`, `scroll`/`scroll.fill`, `arrow.clockwise` (hidden for local TXT), `gearshape`, `ellipsis.circle`; row 2 = `character.bubble` (44×52, bg `textColor.opacity(0.07)` radius 6, blue when on) + book title 16pt bold + chapter title 13pt medium @0.72 + `chevron.down`. Footer height 52 = `chevron.left` (44×44) · `"N/M"` 12pt semibold + `"xx.x%"` 10pt rounded @0.68 · `chevron.right`. Body is justified with `firstLineHeadIndent = fontSize * 1.5`; chapter-title paragraphs are centered, bold, 1.5× size, `padding(.top, 10)` (`ParagraphCardView.swift:24,31,41`). → Any 1px rule added for e-ink is a **new design decision**, not preservation.
- **Reader settings is a sheet, not a screen.** `ReaderSettingsView` is a bare `ScrollView` with **no `NavigationStack`** (see its comment at `:24-25` — that is why its two sub-screens nest sheets instead of `NavigationLink`s).
- **BookDetail's tab bar is a hand-rolled segmented control**, not a system tab bar: `BookDetailView+TabBar.swift` = "Chi tiết" / "Mục lục", underline `Rectangle().fill(accentColor).frame(height: 3)`, plus a `Capsule().fill(Color.red)` badge showing total chapter count next to "Mục lục", on `Color(.systemBackground)`.
- **Shelf has a 4-segment control where two segments are icon-only**: `ShelfTab.swift:43-51` — `downloads` = `arrow.down.circle`, `collections` = `square.grid.2x2` (icon only); `shelf` ("Kệ Sách") and `history` ("Lịch Sử") are text pills. `rawValue` is a cross-screen contract via `userInfo["shelfTab"]` — **never touch it**.
- **SettingsView has no `navigationTitle`** — `NavigationStack { Form { … } }`, title carried by the tab bar. Real section order: "Dịch Thuật Quick Translate" → "Từ điển chung" → `TTSSettingsSection()` → `BrowserSettingsSection()` → `NewChapterSettingsSection()` → `BackupSettingsSection()` → `StaleBookCleanupSettingsSection()` → `DeveloperSettingsSection()` → "Cấu Hình Ghi Log" → "Nhật Ký (Log)" → "Thông Tin". `BackupSettingsSection()` is already a single line here — so the in-flight Telegram work is very likely to consume the one remaining line of headroom.

All of the above is now recorded in the plan as §4c "Đối chiếu với UI thật — các điểm plan từng nói sai".

## Correction #2 — BookDetail tab placement (user caught it again)

User: "màn hình chi tiết chưa giống, 2 tab con chi tiết/mục lục phải hiển thị ở trên ngay dưới header chứ?" — correct, and my second mockup still had it wrong.

Real structure (`BookDetailView.swift:206-232`):

```
.navigationTitle("Chi Tiết Truyện") + .navigationBarTitleDisplayMode(.inline)
VStack(spacing: 0) {
    customTabBar          // :212 — FIRST element, flush under the nav bar
    Divider()             // :214
    TabView(selection:) { // :216 — .tabViewStyle(.page) ⇒ horizontal swipe
        detailTab.tag(0)  // :217
        tocTab.tag(1)     // :220
    }
}
```

- `BookDetailHeaderView` (cover, title, author, source, genres, description) is at `:368` — **inside `detailTab`**, NOT above the tab bar. `detailTab` (`:364`) = `ScrollView { VStack(alignment: .leading, spacing: 16) }` = header → `Divider()` + **Đề xuất** section (`suggests` → `CategoryNovelsListView`) → `Divider()` + **Bình luận** section (`comments` + `CommentSectionView`), with `.refreshable`.
- `tocTab` (`:517`) = chapter list with search / filter / reverse-order controls.
- Nav bar has a trailing `⋯` (`ellipsisMenu`); back button is chevron-only via `NavigationBarAppearance`.

Two e-ink-relevant details:
1. **`TabView(.page)` means the horizontal swipe between the two tabs is a scrolling animation** — same ghosting family as chapter turning. Recorded as risk #13 in the plan; out of scope unless the user extends `instantChapterTurn`.
2. Content is gated behind `renderedTab == selectedTab` with a 0.15 s `DispatchQueue.main.asyncAfter` (`:224-228`, `:366`, `:519`). This is a deliberate anti-double-render optimisation — **do not touch it** while doing e-ink work.

## Correction #3 — read all four tabs properly (user's explicit request)

User: "bạn cần đọc kỹ các tab shelf, discovery, extension, setting lại để đảm bảo UI mock thật chứ không phải là đoán. Điều này cũng tránh khỏi khi thực hiện toàn làm UI kiểu đoán."

So I read each tab's real body. Findings, now in the plan as §4c.7–§4c.11 and §4b.8:

**Shelf** (`ShelfView.swift:137-256`, `ShelfTabSelectorView.swift`) — the tab selector is a **row of separate buttons, NOT a segmented control**: `HStack(spacing: 8)` + `Spacer(minLength: 0)` (left-aligned, not evenly divided). `Downloads`/`Bộ Sưu Tập` = 40×40 circular icon buttons; `Kệ Sách`/`Lịch Sử` = text capsules (`.subheadline.weight(.medium)`, padding h14 v9). Selected = `fill(accentColor.opacity(0.15))` + `strokeBorder(accentColor, 1.5)` + `accentColor` text; unselected = `fill(Color(.secondarySystemBackground))` + `strokeBorder(secondary.opacity(0.25), 1)` + `.secondary` text. Selection change animates `easeInOut(0.18)`. Nav title is **inline**; toolbar leading = bell with a red capsule badge offset x:10 y:-8; trailing = magnifier (hidden on Downloads) + `ellipsis.circle` menu.

**Discovery** (`DiscoveryView.swift:98-346`) — **no navigation bar at all** (`.toolbar(.hidden, for: .navigationBar)` at `:346`), and **no title anywhere**. Custom header = source **pill** (`cornerRadius 18`, `secondarySystemBackground`) with `ExtensionIconView(size: 24)` + name + `chevron.down`, then 3 circular buttons (`padding(10)`, `.title3`, `Circle()`): `safari` (only if `sourceUrl` non-empty), `character.bubble` toggle, `magnifyingglass` → `SearchView`. Then a genre strip: grid button (`circle.grid.2x2.fill`, `accentColor.opacity(0.1)`, `cornerRadius 8`) + horizontal chips (selected = `accentColor` bg + white bold text; unselected = `gray.opacity(0.1)` + primary text; radius 20), then `Divider()`. Content = `TabView(.page)` of `DiscoveryCategoryTabView`. Three real empty states: no extensions → `safari` 60pt; error → `exclamationmark.triangle.fill` 48pt **orange**; genres-only → `circle.grid.2x2` 48pt `accentColor` + accent pill button.

**Extensions** (`RepositoryManagerView.swift:77-109`, `extensionRow` at `:368`) — uses a **real `Picker(.segmented)`** with `Tất cả tiện ích` / `Danh sách kho` on `systemGroupedBackground` (different from Shelf!). `navigationTitle("Kho Tiện Ích")` inline. Same `renderedTab` + `asyncAfter(0.15s)` gate. Row content: `ExtensionIconView(44)` (fallback icon 32pt + padding 6 + `secondary.opacity(0.2)` bg + `accentColor` fg + radius 8), name `.headline`, version badge (`orange.opacity(0.15)`/orange if update else `blue.opacity(0.1)`/blue), **a real flag emoji**, then three `.system(size: 9)` badges — type (`translateType`: "Truyện chữ" / "Truyện Trung" / "Giọng đọc (TTS)") on `orange.opacity(0.12)` or `purple.opacity(0.12)`, author on `green.opacity(0.1)`, `debug` on `red.opacity(0.12)` — then `sourceUrl` in `.caption2` gray.

**Flag emoji finding (new)**: `RepositoryManagerView+Actions.swift:186` `getFlagEmoji(_:)` → `vi`→🇻🇳, `zh`/`cn`→🇨🇳, `en`→🇺🇸, else 🌐. Colour glyphs cannot render on a monochrome e-ink panel — silent failure, build stays green. Must become text codes `VI`/`ZH`/`EN`/`—`. Checked: **no Taiwan/HK/Macao mapping**, so no sovereignty concern; `en`→🇺🇸 is just a design choice.

**Settings** — I was **wrong** in correction #2: `SettingsView.swift:308-309` DOES have `.navigationTitle("Cài Đặt")` + `.navigationBarTitleDisplayMode(.inline)`. I had only grepped the first 24 lines. Real section order (11): "Dịch Thuật Quick Translate" → "Từ điển chung" → "Nghe Truyện (TTS)" → "Trình Duyệt Hiển Thị" → "Chương Mới" → "Sao Lưu & Khôi Phục" → "Dọn Dẹp Thư Viện" → `DeveloperSettingsSection()` → "Cấu Hình Ghi Log" → "Nhật Ký (Log)" → "Thông Tin". `DeveloperSettingsSection` uses a **footer** `Text("Nhà Phát Triển")`, not a header.

### Process convention adopted
Created `.workbuddy-ai/memory/MEMORY.md` recording the standing rule: **never guess UI — read the real view and cite `file:line`; mockups can only be structural wireframes here** (no screenshots in repo, no Simulator on Windows), and say that limitation out loud. Also recorded the real navigation structure, the two different tab-selector styles, and the `renderedTab` gate that must not be touched.

## Backup feature work — local cleanup (code changed)

Two user requests implemented in the Backup subsystem, then full CodeGraph bookkeeping.

**1. "Xoá tất cả bản sao lưu trong máy" button.** Three layers, following the existing structure:
- `LocalBackupStore.deleteAll()` (file layer) — iterates `list()` and removes each archive rather than removing the directory, because `backups/` also holds in-flight worker temp files. Continues past failures, then throws `Failure.deleteAllPartial(deleted:failed:)` (new enum case) so a partial result is reported honestly instead of swallowed.
- `BackupCoordinator.deleteAllLocal()` — guards on `isBusy`, calls `refreshLocal()` *before* setting `lastError` so the UI matches reality.
- `LocalBackupListView` — new destructive row + confirmation dialog. Refactored the delete state into a single `DeleteTarget` enum (`one(item)` / `all`) instead of two competing booleans; the dialog stays attached to the **footer** leaf view per the file's own documented rule.

**2. Delete the local file after a successful upload.** `uploadToDrive` / `uploadToTelegram` previously only set `lastMessage` — neither deleted the local archive (verified by reading both). Added `removeLocalCopyAfterUpload(_:)`, called **only on the success branch**; on delete failure the message says "(chưa xoá được bản trong máy)" rather than staying silent. Confirmed the only call sites are `BackupHubView:49-50`. Deliberate consequence documented: a manual upload is one archive → one destination, because the local copy is gone after the first upload — multi-destination needs the automatic path.

### Trap hit and fixed: the 400-line ceiling
Adding both functions to `BackupCoordinator.swift` pushed it to **402**/400 and produced a genuine `NEW_FILE_TOO_LARGE` violation — my own, so I had to fix it rather than loosen anything. Resolved the repo's documented way: split into `Sources/Services/Backup/BackupCoordinator+LocalCleanup.swift` (50 lines, extension only). Coordinator back to **361**; `check_architecture.py` returned to exactly the **6** pre-existing line-limit violations.

Notably this extension needs **no** internal doors (`setBusy`/`setProgress`) unlike `+AutoDrive`, because it only *reads* `isBusy`/`localBackups` and calls `refreshLocal()`.

### CodeGraph bookkeeping (full cycle done)
A new Swift file made **five** docs stale at once: `00_index`, `02_file_graph`, `09_dependency_rules`, `14_complexity_report` (all `staleOn: structure`) plus `11_subsystems` (content). Added a 1.3.356 section to each GENERATED zone, then `--accept 00 02 09 11 14`. Final `validate_links.py` → **PASS: 16 documents, 524 Swift files**.

Also hit the CHANGELOG archival threshold: 34 entries → moved the 4 oldest (`1.3.322`..`1.3.319`) into `CHANGELOG.archive.md` with a script, preserving totals (30 + 294 = 324 = 34 + 290). Archive order stays newest-first.

Final line counts: `BackupCoordinator.swift` 361, `BackupCoordinator+LocalCleanup.swift` 50, `LocalBackupStore.swift` 132, `LocalBackupListView.swift` 181.

**Not verified:** Windows host has no Swift/Xcode, so nothing was compiled. A new Swift file means `xcodegen generate` is required before building on macOS. No tests in this repo.

## Commit + push attempt (push BLOCKED)

Committed the backup cleanup work as **`08a855d`** on branch `sigle_reader` — 12 files, +267/−95. Subject matches the CHANGELOG 1.3.356 title per repo convention. Excluded from the commit on purpose: `opencode.json` (unrelated local AI-provider config), `.workbuddy-ai/` (assistant workspace), `Scripts/__pycache__/`.

Also fixed a whitespace gate failure before committing: my CHANGELOG archival script left a blank line at EOF (`git diff --check` → "new blank line at EOF"), stripped with `rstrip() + '\n'`.

**Push does not work in this environment — root cause found:**
- `credential.helper = helper-selector` (set at repo *and* system level) → Git Credential Manager tries to open a **GUI** login prompt, which never returns here, so the process gets SIGTERM with **no output at all**. This is why the first three push attempts died silently.
- Confirmed by bypassing the helper: `git -c credential.helper= push` → `fatal: could not read Username for 'https://github.com': terminal prompts disabled`. So it is purely a missing-credentials problem, not a sandbox or network problem.
- `git ls-remote origin` **works** unauthenticated (repo is publicly readable) and shows the remote `sigle_reader` still at `a6f7a47`, i.e. my commit is local-only.
- No `GITHUB_TOKEN`/`GH_TOKEN` in env, no `~/.git-credentials`, and `gh auth status` → not logged in. So CI cannot be monitored via `gh run` either.
- **Useful:** the GitHub REST API answers unauthenticated (`https://api.github.com/repos/buudvh/FreeBook/actions/runs` → 200), so CI *can* be polled with `curl` once the commit is actually pushed — no `gh` login needed for that.

CI workflow facts: `.github/workflows/build-ipa.yml`, name "Build Unsigned IPA", runs on `macos-15`, triggers on push to `main`/`sigle_reader` when `Sources/**` or `project.yml` change. It only does `xcodegen generate` → `xcodebuild archive` (unsigned) → package IPA + inject `espeak-ng-data` → Telegram. It runs **no tests, no validators**, so green CI only proves it compiles.

Note: `Docs/Plans/**` is gitignored, so the e-ink plan document is intentionally not committed.

## E-Ink UI color sweep + background-color option (code changed, committed)

User asked to fix every remaining **colored** UI spot into e-ink style, AND add a background-color picker (White #F2F1EC / Gray #D8D8D2 default / Warm #E5DED0).

**The earlier CI compile failure is already fixed** — `git log` shows `d633924` "Sửa lỗi biên dịch chế độ E-Ink" on top of the original `d4c89a3`. So the build is healthy; my new work sits on `8e88e76` (committed locally, NOT pushed — user pushes via opencode).

### Approach that worked (reusable)
- **`EInkPaperColor` enum nested inside `EInkModeSettings`** (NOT a top-level type — file must stay at 1 type per `MULTI_PRIMARY_TYPES`), and `Services` tier forbids `import SwiftUI`, so the enum only carries `Int` raw + a Vietnamese `label`. Color conversion lives in `EInkPalette` (which imports SwiftUI/UIKit).
- **`EInkPalette.paper` + `paperUIColor` changed from `let` constants to computed `var`** reading `EInkModeSettings.shared.paperColor`. Because `EInkAppearance` (in `Common/Utils/`) already consumes `paperUIColor` for the nav/tab bar proxy, and `EInkModeSettings.setPaperColor` calls `EInkAppearance.apply()`, the chosen tone propagates to every e-ink surface (panels, badges, Shelf tab selector, `ReaderView` `.eink` theme, FlashView) with no other wiring.
- **Colored-spot fix pattern:** accents → `.einkAccentForeground(fallback)` (self-contained, reads `@AppStorage` internally, so the call site needs no extra state); filled-circle/selection backgrounds → swap `Color.x.opacity(0.1)` for `EInkPalette.paper` + `Capsule()/Circle().strokeBorder(EInkPalette.ink)`; destructive/selected text → invert (black fill, white text). `FloatingSelectionMenu` "Xoá" is on a dark bubble, so in e-ink it becomes white + dashed underline (not black, which would be invisible on dark).
- Files touched (12, all ≤400 lines, no new files): `EInkModeSettings`, `EInkPalette`, `EInkSettingsSection`, `BookListItemView`, `ShelfView`, `BookActionSheet`, `ReaderDefinitionOverlayView`(+`+Rules`), `ReaderRuleTraceChip`, `ReaderJunkDeleteOverlayView`, `TTSQuickTimerSheet`, `FloatingSelectionMenu`.
- `check_architecture.py` stayed at the **6** pre-existing line-limit violations — no new ones. `validate_links.py` → PASS 16 docs (used `--no-change-needed` on 5 stale docs because the Antigravity generator isn't available locally; their GENERATED content is still accurate). CHANGELOG `1.3.359`; rolled 3 oldest (1.3.323–325) into archive to hold 30.

**Not verified:** Windows host has no Swift/Xcode, so still uncompiled. CI (build-ipa.yml, macOS, unsigned archive only, no tests) is the only compile gate and will run once the user pushes `8e88e76` via opencode.






